import { createMuiTheme } from "@material-ui/core/styles";

export const lightTheme = createMuiTheme({
  palette: {
    primary: {
      main: "#4eb8ea",
      contrastText: "#ffffff",
    },
  },
  overrides: {},
  props: {},
});
